{
  "name": "nano-cloud-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Cryptographic proxy tier — OpenRouter LLM routing + Supabase service-role transaction management for Nano Cloud Codec",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.4.0"
  }
}
